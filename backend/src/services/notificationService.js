

export function notifyUser(userId, message) {

  console.log(`Notify user ${userId}: ${message}`);
}

export function notifyProvider(providerId, message) {
  console.log(`Notify provider ${providerId}: ${message}`);
}
